# services/scheduler

Scheduling / dispatching rules engine for UltraKIL. Owned by Chanya.

Implements the hard/soft scheduling constraints (service area matching, stationed staff,
PMS-grade supervisor coverage, allowed vs. preferred days, opening hours, Unassigned queue with
reasons, override/lock handling).
