# Source-controlled global staging entrypoint. It imports the isolated
# UltraKIL site fragment so hosts with unrelated Caddy sites can retain their
# existing global file and import only deploy/caddy/Caddyfile.ultrakil.
{
	email admin@taskforceai.tech
}

import Caddyfile.ultrakil
