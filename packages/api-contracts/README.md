# packages/api-contracts

Shared OpenAPI contract and generated API client for UltraKIL. Owned by Chanya.

`apps/manager-web` consumes the generated client from this package instead of manually
duplicating backend types. Keep the contract updated here before starting frontend integration
work for a given endpoint.
