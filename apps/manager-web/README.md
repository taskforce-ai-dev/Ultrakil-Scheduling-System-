# apps/manager-web

Manager portal frontend for UltraKIL (customer/service agreement workflow, calendar, dispatch
board, overrides, manager documentation). Owned by Oshadi.

Must consume the generated API client from `packages/api-contracts` rather than manually
duplicating backend types.
